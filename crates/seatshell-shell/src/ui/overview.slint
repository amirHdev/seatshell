import { UserCard } from "user-card.slint";
import { ScrollView } from "std-widgets.slint";
import { ShellTheme } from "shell-theme.slint";
import { ShellAction, ShellPill } from "shell-widgets.slint";

export struct UserSession {
    username: string,
    state: string,
    action: string,
    selected: bool,
    locked: bool,
    current: bool,
}

export component Overview inherits FocusScope {
    in property <string> accent: "#4a90e2";
    in property <[UserSession]> sessions;
    in property <string> status-text;
    in property <bool> phone: root.width < 520px;
    in property <bool> compact: root.width < 860px;
    in property <bool> short: root.height < 560px;
    in property <bool> wide: root.width >= 1120px && !root.short;
    callback move-selection(int);
    callback activate-current-session();
    callback close();

    key-pressed(event) => {
        if (event.text == Key.UpArrow) {
            root.move-selection(-1);
            accept
        } else if (event.text == Key.DownArrow) {
            root.move-selection(1);
            accept
        } else if (event.text == Key.Escape) {
            root.close();
            accept
        } else if (event.text == Key.Return) {
            root.activate-current-session();
            accept
        } else {
            reject
        }
    }

    Rectangle {
        width: parent.width;
        height: parent.height;
        background: ShellTheme.background;

        Rectangle {
            x: -80px;
            y: 28px;
            width: 260px;
            height: 260px;
            background: ShellTheme.ambient-primary;
            border-radius: 999px;
            opacity: 0.26;
        }

        Rectangle {
            x: root.width - 220px;
            y: root.height - 240px;
            width: 280px;
            height: 280px;
            background: ShellTheme.ambient-secondary;
            border-radius: 999px;
            opacity: 0.24;
        }

        VerticalLayout {
            padding: 14px;
            spacing: 12px;

            Rectangle {
                height: root.compact
                    ? root.short ? 136px : 168px
                    : root.short ? 104px : 128px;
                background: transparent;

                if !root.compact: HorizontalLayout {
                    padding: 16px;
                    spacing: 12px;
                    alignment: center;

                    VerticalLayout {
                        spacing: 4px;
                        horizontal-stretch: 1;

                        Text {
                            text: "Seat Overview";
                            color: ShellTheme.text-primary;
                            font-size: 20px;
                            font-weight: 800;
                        }

                        Text {
                            text: "Switch between local sessions.";
                            color: ShellTheme.text-secondary;
                            font-size: 12px;
                            wrap: word-wrap;
                        }

                        if !root.short: Text {
                            text: root.status-text;
                            color: ShellTheme.text-muted;
                            font-size: 11px;
                            wrap: word-wrap;
                        }
                    }

                    ShellPill {
                        label: root.sessions.length + " sessions";
                        compact: true;
                    }

                    ShellAction {
                        label: "Activate";
                        icon-name: "command";
                        compact: true;
                        clicked => {
                            root.activate-current-session();
                        }
                    }

                    ShellAction {
                        label: "";
                        icon-name: "close";
                        compact: true;
                        clicked => {
                            root.close();
                        }
                    }
                }

                if root.compact: VerticalLayout {
                    padding: 14px;
                    spacing: 8px;

                    Text {
                        text: "Seat Overview";
                        color: ShellTheme.text-primary;
                        font-size: 18px;
                        font-weight: 800;
                    }

                    Text {
                        text: "Switch between local sessions.";
                        color: ShellTheme.text-secondary;
                        font-size: 11px;
                        wrap: word-wrap;
                    }

                    if !root.short: Text {
                        text: root.status-text;
                        color: ShellTheme.text-muted;
                        font-size: 11px;
                        wrap: word-wrap;
                    }

                    HorizontalLayout {
                        spacing: 8px;

                        ShellPill {
                            label: root.sessions.length + " sessions";
                            compact: true;
                            tiny: root.phone;
                        }

                        ShellAction {
                            label: root.phone ? "" : "Activate";
                            icon-name: "command";
                            compact: true;
                            tiny: root.phone;
                            clicked => {
                                root.activate-current-session();
                            }
                        }

                        ShellAction {
                            label: "";
                            icon-name: "close";
                            compact: true;
                            tiny: root.phone;
                            clicked => {
                                root.close();
                            }
                        }
                    }
                }
            }

            if root.wide: HorizontalLayout {
                vertical-stretch: 1;
                spacing: 12px;

                Rectangle {
                    width: 300px;
                    background: #11192780;
                    border-radius: ShellTheme.radius-xl * 1px;

                    VerticalLayout {
                        padding: 14px;
                        spacing: 12px;

                        Text {
                            text: "Session controls";
                            color: ShellTheme.text-primary;
                            font-size: 15px;
                            font-weight: 800;
                        }

                        Text {
                            text: "Select a local session and activate it without leaving the shell.";
                            color: ShellTheme.text-secondary;
                            font-size: 11px;
                            wrap: word-wrap;
                        }

                        Rectangle {
                            background: transparent;

                            VerticalLayout {
                                padding: 12px;
                                spacing: 8px;

                                Text {
                                    text: "Keyboard";
                                    color: ShellTheme.text-secondary;
                                    font-size: 12px;
                                    font-weight: 700;
                                }

                                Text {
                                    text: "Use Up and Down to move. Press Enter to activate the selection.";
                                    color: ShellTheme.text-muted;
                                    font-size: 11px;
                                    wrap: word-wrap;
                                }
                            }
                        }

                        Rectangle {
                            background: transparent;

                            VerticalLayout {
                                padding: 12px;
                                spacing: 8px;

                                Text {
                                    text: "States";
                                    color: ShellTheme.text-secondary;
                                    font-size: 12px;
                                    font-weight: 700;
                                }

                                Text {
                                    text: "Current, locked, and inactive sessions use distinct colors.";
                                    color: ShellTheme.text-muted;
                                    font-size: 11px;
                                    wrap: word-wrap;
                                }
                            }
                        }

                        Rectangle {
                            vertical-stretch: 1;
                            background: transparent;
                        }
                    }
                }

                Rectangle {
                    horizontal-stretch: 1;
                    background: #11192780;
                    border-radius: ShellTheme.radius-xl * 1px;
                    border-width: 1px;
                    border-color: ShellTheme.outline;

                    VerticalLayout {
                        padding: 14px;
                        spacing: 10px;

                        Text {
                            text: "Local sessions";
                            color: ShellTheme.text-primary;
                            font-size: 16px;
                            font-weight: 800;
                        }

                        if root.sessions.length == 0: Rectangle {
                            background: ShellTheme.background-elevated;
                            border-radius: ShellTheme.radius-lg * 1px;
                            border-width: 1px;
                            border-color: ShellTheme.outline;

                            VerticalLayout {
                                padding: 16px;
                                spacing: 6px;
                                alignment: center;

                                Text {
                                    text: "No sessions available";
                                    color: ShellTheme.text-primary;
                                    font-size: 15px;
                                    font-weight: 700;
                                    horizontal-alignment: center;
                                }

                                Text {
                                    text: "SeatShell could not read session data. Start the admin service or switch to the development fallback.";
                                    color: ShellTheme.text-secondary;
                                    font-size: 11px;
                                    wrap: word-wrap;
                                    horizontal-alignment: center;
                                }
                            }
                        }

                        ScrollView {
                            vertical-stretch: 1;
                            viewport-width: self.width;

                            VerticalLayout {
                                spacing: 10px;

                                for session in root.sessions: UserCard {
                                    username: session.username;
                                    state: session.state;
                                    action: session.action;
                                    selected: session.selected;
                                    locked: session.locked;
                                    current: session.current;
                                    accent: root.accent;
                                    compact: root.compact;
                                    tiny: root.width < 380px;
                                }
                            }
                        }
                    }
                }
            }

            if !root.wide: Rectangle {
                vertical-stretch: 1;
                background: ShellTheme.surface;
                border-radius: ShellTheme.radius-xl * 1px;
                border-width: 1px;
                border-color: ShellTheme.outline;

                VerticalLayout {
                    padding: 14px;
                    spacing: 10px;

                    Text {
                        text: "Local sessions";
                        color: ShellTheme.text-primary;
                        font-size: 16px;
                        font-weight: 800;
                    }

                    Text {
                        text: "Use Up and Down to move through sessions. Press Enter to activate the selected session.";
                        color: ShellTheme.text-secondary;
                        font-size: 11px;
                        wrap: word-wrap;
                    }

                    if root.sessions.length == 0: Rectangle {
                        background: ShellTheme.background-elevated;
                        border-radius: ShellTheme.radius-lg * 1px;
                        border-width: 1px;
                        border-color: ShellTheme.outline;

                        VerticalLayout {
                            padding: 16px;
                            spacing: 6px;
                            alignment: center;

                            Text {
                                text: "No sessions available";
                                color: ShellTheme.text-primary;
                                font-size: 15px;
                                font-weight: 700;
                                horizontal-alignment: center;
                            }

                            Text {
                                text: "SeatShell could not read session data. Start the admin service or switch to the development fallback.";
                                color: ShellTheme.text-secondary;
                                font-size: 11px;
                                wrap: word-wrap;
                                horizontal-alignment: center;
                            }
                        }
                    }

                    ScrollView {
                        vertical-stretch: 1;
                        viewport-width: self.width;

                        VerticalLayout {
                            spacing: 10px;

                            for session in root.sessions: UserCard {
                                username: session.username;
                                state: session.state;
                                action: session.action;
                                selected: session.selected;
                                locked: session.locked;
                                current: session.current;
                                accent: root.accent;
                                compact: root.compact;
                                tiny: root.width < 380px;
                            }
                        }
                    }
                }
            }
        }
    }
}
